## What this changes

## Why

## How I tested it
- [ ] `./run.sh` against a scratch library
- [ ] `cd frontend && npx tsc --noEmit && npm run build`
- [ ] Desktop bundle (if it touches the sidecar or Tauri)

## Privacy check
- [ ] Adds no outbound network call that carries user audio, transcripts or names
