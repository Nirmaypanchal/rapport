# 2026-09-14 · Research — first full run: escalation sweep, feedback, market, trends, board restock

**Set out to:** the escalation sweep, user feedback, market notes, trends, and leave at least seven unblocked Ready
items in `backlog.md` — Now had held exactly one item, blocked on the owner, since 09-13, and Build has fallen
through to Next for two days running.

## Escalation sweep

Re-checked all four open `needs-human` issues against reality rather than the file dates:
- **#12 (nightly silent)** — still no `sprint/log/*-nightly.md` newer than 09-10. Five days silent now. Still open.
- **#5 (Reddit network block)** — re-ran the exact check from the escalation file: `curl -m 10
  https://www.reddit.com/r/rapport/new.json` → `CONNECT tunnel failed, response 403`, and the agent proxy status
  confirms `connect_rejected` on `www.reddit.com:443`. Unchanged. Still open.
- **#3 (Reddit bot credentials)** and **#14 (Apple signing)** — no comments on either issue since filing (checked
  via `issue_read get_comments`), so no owner decision to act on. Still open.

Nothing closed, nothing moved to `needs-human/done/`, nothing re-escalated (no new information on any of the four).

## User feedback and market

The repository itself has no external feedback yet: 7 issues and 0 discussions total, every one authored by
`github-actions` or the owner's own account closing agent-filed escalations. Recorded as a fact, not a gap, in
`sprint/research/feedback.md`.

With nothing from our own users, read what people say about the products on the watch list instead (dated entries in
`sprint/research/feedback.md` and `sprint/research/competitors.md`): Granola's top complaint is silent capture
failure, not a missing feature; Granola and Notion AI Meeting Notes share the identical "wall of text past two
speakers" complaint, which is exactly what Rapport's diarization already solves; Otter is in litigation over its
recording bot and Fireflies faces BIPA suits; Limitless was acquired by Meta and shut down around its users;
Granola and Otter both shipped their own MCP servers this year (Rapport's has been live since 09-11); and a new
direct open-source competitor, Note67, launched on Hacker News. One factual fix followed from this:
`docs/comparison.md`'s MCP row said "No" for Otter and Granola, which stopped being true.

## Trends

Created `sprint/research/trends.md` (it didn't exist; flagged missing at last week's retro). Sized Parakeet-on-MLX
(queued from week 37) at **M** — a measured comparison against Whisper on the owner's own Mac, not a swap, since
every number is someone else's benchmark. Found a real architecture question — Argmax's WhisperKit now bundles a
Swift diarization SDK that does the same job as Rapport's Python diarizer — and deliberately did **not** promote it
to the backlog: it either means a second Swift process or reopening the "Python backend" decision, and that call
isn't mine to make unilaterally. Wrote it into `sprint/decisions.md` and flagged it for the Retrospective instead.
Two smaller, lower-confidence trends (a default local-LLM doc recommendation, Apple's Foundation Models framework as
a possible third provider) went to `backlog.md` Next rather than Now — the second is explicitly the least-verified
claim in the file and shouldn't be built against without confirming the API exists as described.

## Plan

`backlog.md` Now went from one blocked item to **ten** Ready items, each with a user story, acceptance criteria, UI
notes and files likely touched:

1. Notice when the nightly goes quiet (S)
2. Stop CI leaving a red ghost run after every auto-merge (S)
3. Make `sprint-merge` fail loudly (S)
4. An `ask` tool for MCP (S)
5. Stream the answer while Ask is thinking (S)
6. The Search page should find summaries too (S)
7. One vocabulary for sources (S)
8. Zoom / Google Meet local recordings (S)
9. Obsidian and Markdown export of the whole library (S)
10. MCP server, slice 2: write-back, notes only (M)
11. Signed, notarized releases — kept, moved to the bottom since it's still blocked on the owner and Build takes the
    first unchecked item literally.

Items 4, 6, 7 and 10 directly answer questions Build queued in `messages.md` (the search results view, the source
vocabulary question asked twice, and MCP write-back scope asked twice); the reasoning behind each is recorded in
`sprint/decisions.md` (four new entries, dated 2026-09-14) rather than only in the backlog line, since a future
reader would question all four. `docs/roadmap.md` updated to match (Search-finds-summaries, Zoom/Meet, Obsidian
export moved from Later to Next). Read all four items' code paths myself before speccing them (`rapport/mcp.py`,
`rapport/db.py`, `rapport/ask.py`, `rapport/server.py`'s `fs_roots`, `sources-view.tsx`, `search-view.tsx`,
`ask-panel.tsx`) rather than guessing at file names, and used Mobbin once (Dropbox Dash, Tana) to confirm the
single-list-with-a-badge pattern for the Search spec has real precedent.

## What is open

- The nightly is five days silent; not re-escalated (no new information), and its own fix (item 1 above) is now
  Ready for Build to take.
- The WhisperKit/SpeakerKit architecture question is unresolved by design — it's a decision for the Retrospective or
  the owner, not something I should settle alone.
- The Apple Foundation Models trend needs someone with an actual macOS session to confirm the API before it's worth
  more than a Next-list line.

## What the next run should do

1. **Build:** take the top of Now — it's real work now, not a fallback to Next.
2. **Community/Release:** three messages above have marketing material (Otter/Fireflies/Limitless positioning, the
   MCP-row fix) — nothing to build, something to say, when a release exists to announce.
3. **Me, next Monday:** whether the ten Ready items lasted the week or Build burned through them faster than
   expected; whether any of the four re-verified escalations finally got an owner reply.
