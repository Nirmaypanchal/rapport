# 2026-09-13 · Retrospective — the first round table (week 37)

**Set out to do:** read everyone's first week, write the round table, improve the role files and skills, look outside,
tidy the board, escalate what needs a human.

**Read:** AGENTS.md, `sprint/README.md`, all six role files, all 13 log entries, `messages.md` (26 entries),
`backlog.md`, `decisions.md`, all five skills, `needs-human/` (4 files), `reddit/` (empty), `research/` (untouched since
bootstrap), `marketing/`; `git log --since='8 days ago'`, and live GitHub state — 7 PRs (all merged, 0 open), 6 issues
(4 open), 45 workflow runs, 0 tags.

## Facts

| | Runs | Shipped | Left behind |
|---|---|---|---|
| Build | 5 of 5 days | 6 PRs, all squash-merged, CI green; tests 34 → 119 | nothing red, nothing open |
| Community | 5 of 5 days | 0 posts, 0 replies, 0 issues filed; 1 real finding | 5 near-identical logs |
| Release | 1 of 1 Friday | no tag; landing page and `docs/comparison.md` made true | 3 features unreleased |
| Nightly | 1 of 4 nights | 1 FAIL, correct and precise | silent 09-11, 09-12, 09-13 |
| Research | 0 (first run 09-14) | — | Now down to one blocked item |

Guardrails bent: **none**. One honored notably: Build caught that the MCP server punctures the product's central privacy
claim and fixed `docs/privacy.md` the same day rather than shipping quietly.

Four CI runs are red. All four are zero-job ghosts created when `sprint-merge` opens a PR that merges three seconds
later; the same SHAs are green on `push`. No real CI failure occurred this week.

**The finding that reframes the rest: nothing has ever been released.** No tag, `/releases` empty, and the landing
page's three "Download for Mac" buttons lead to an empty page — including through Release's own 09-11 edit of that
file. Nothing to announce → Community has nothing to post; no users → nobody for Community to listen to and no feedback
for Research to plan from. Four of five agents are idling downstream of one missing artifact.

**The structural finding:** Build takes one Ready item a day; Research promotes "at most two" a week. That arithmetic
ended on 09-13 with Now holding a single item blocked on the owner.

## What I changed

- **`sprint/retro/2026-37.md`** — the round table: one section per agent in their voice, what I found outside the loop,
  seven decisions, and what I will check next Sunday.
- **Role files** (all six, each dated in its own changelog): Research gains an escalation sweep as step 1 and a
  seven-unblocked-Ready floor (cap on promotions removed); Build gains a fall-through when Now is blocked and a rule to
  mark guesses as guesses in backlog notes; Release gains the gate expiry, escalate-in-the-run-a-gate-blocks-you, and a
  check that the download link leads somewhere; Community gains a quiet-run fast path and the Discussions method;
  Nightly gains the two things only it can do; Retro gains "look at the product from outside the loop".
- **Skills**: new `community-listening.md`. New `all-sprint-automation.md` — split out of `build-codebase-patterns.md`
  (the only skill over a page, and named so only Build would read it, even though the week's one real Community finding
  was a workflow bug); both files are now under a page. `reddit.md` rewritten (four stacked "still blocked" entries →
  one current statement). `release-checklist.md` gains the gate expiry and the empty-`/releases` trap.
  `all-cloud-environment.md` re-verified, plus WebFetch-on-GitHub-pages and the MCP `fields` gotcha.
- **`sprint/decisions.md`**: the nightly gate expiry, and "a weekly agent may not own a daily unblock".
- **Backlog**: a nightly-silence watchdog (S, with the `scripts/` + tests shape spelled out), a fix for the ghost CI
  runs (S, with the exact job condition — note `pull_request: {branches:}` filters the *base* branch and cannot do it),
  and a Parakeet-on-MLX evaluation (M, for Research to size). Recorded that Now is blocked and what Build should take.
- **`docs/use-cases.md`**: "Nothing leaves your machine, which matters for sources" was the old absolute, unqualified,
  in the paragraph aimed at journalists protecting sources — the one place it actually matters. Now matches
  `docs/privacy.md`.
- **Tidy**: `messages.md` archived to `messages-archive/2026-37.md`, leaving five genuinely open threads plus this
  run's notes. Issue #2 closed with the evidence (five PRs have auto-merged since the owner flipped the setting on
  09-10) and its file moved to `needs-human/done/` — it had been resolved in fact for four days while three Community
  runs flagged it into a queue with no consumer.

## Outside the loop

`parakeet-mlx` is a real alternative to Whisper on Apple silicon — lower WER on clean English for less compute, and it
streams, which Whisper cannot. On the board as a measured comparison, not a swap, with links and a caveat that the
numbers are other people's benchmarks until the owner's Mac reproduces them.

A lead I deliberately did **not** write into a skill: cloud routines can reportedly be triggered by a GitHub event, not
just a schedule. That would fix both structural problems above, but all I have is a third-party summary, and a skill
that is wrong is worse than none. It is a footnote in the escalation, not an ask.

## Escalations

One new file: `needs-human/2026-09-13-apple-developer-credentials.md`. The top item under Now has been blocked all week
on credentials nobody had asked for — the backlog said "see `needs-human/`" and pointed at a file that did not exist.
It is also a money decision ($99/year), so it is the owner's either way. Written so that **either** answer unblocks the
board: add the five secrets, or say "not now" and we ship unsigned with an honest first-launch note. Emailed per the
routine's rule (one email, one address).

Not re-escalated: #3, #5 and #12 are open and unchanged, and AGENTS.md says not to escalate twice for the same thing.
#12 is referenced inside the new file as the other ten-minute job only the owner can do.

**Verified live after the push**, which is the only way to check this: the `Needs human` run opened exactly one issue —
[#14](https://github.com/Nirmaypanchal/rapport/issues/14) — and skipped the three older files. Build's dedupe fix
([#11](https://github.com/Nirmaypanchal/rapport/pull/11)) is holding a week on. CI green on `main`
([run 36](https://github.com/Nirmaypanchal/rapport/actions/runs/34774379659)), `scripts/test-light.sh` 119 passed
locally before the commit. One email sent, to hi@nirmaypanchal.com, carrying the escalation file.

## What the next runs should do

1. **Research (Monday, first run):** the escalation sweep, then fill Now with seven unblocked Ready items. Two specs are
   already queued in `messages.md` (MCP write-back, the Search-page result list) and `sprint/research/trends.md` does
   not exist yet.
2. **Build:** if Now is still blocked when you run, take **Summary template per source** (S) and say so — that is the
   new rule, not an improvisation. The watchdog and ghost-CI items are both small and both yours.
3. **Release (Friday):** if the nightly has reported, tag — three features are waiting. If it has not, the gate expires
   on 09-17 (seven days after the 09-10 log) and the three conditions in `release.md` step 1 apply. Either way, open the
   download link.
4. **Me (next Sunday):** whether a tag exists; whether the nightly reported; whether Research's seven Ready items
   survived the week; whether Community's quiet runs got shorter and its listening wider; and whether closing
   escalations on evidence cleared the backlog or just moved the waiting somewhere else.
