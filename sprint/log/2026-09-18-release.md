# 2026-09-18 · Release & marketing

**Set out to do:** decide whether to tag, release if so, refresh docs/landing page, draft announcements, weekly email.

## Decision: no release this run

No tag exists yet (`git describe --tags --abbrev=0` → none), and seven user-visible features have shipped to `main`
unreleased since 0.1.0 never happened: Ask your library (#7), the MCP server (#10), Ask reading summaries (#13), a
summary template per source (#15), the MCP `ask` tool (#22), Ask streaming its answer (#26), and Search finding
summaries too (#27) — all listed under `CHANGELOG.md`'s `[Unreleased]`.

CI on `main` is green on the current head (`dd7e264`, run
[35302633255](https://github.com/Nirmaypanchal/rapport/actions/runs/35302633255), conclusion `success`). But the
nightly gate still blocks, and this is not the "expired gate" case my role file added on 2026-09-13:

- The newest `sprint/log/*-nightly.md` is still `2026-09-10-nightly.md`, and its verdict is **FAIL**. Nine days have
  passed with no new nightly log (`sprint/messages.md`, Build, 09-18).
- The seven-day expiry clause exists for a machine that has **stopped reporting**, not for one whose last real
  report was a failure — the role file says so explicitly: "Never use this to skip a nightly that ran and *failed*."
  `sprint/skills/release-checklist.md` makes the same point more sharply: "don't release even when the cause is
  already fixed and merged on `main` — a fresh nightly run is the only thing that has ever caught a real pipeline
  bug," and a brand-new subsystem nightly has never touched (here: the MCP server, and now also `ask` streaming's
  MLX path, per Build's 09-17 note that it "cannot be run here at all") is "a real gap worth naming even when
  everything else is green."
- Read closely, the 09-10 FAIL was a harness bug (`scripts/e2e.py` had no `__main__` guard and read a stale API key),
  fixed for real the same day in #9, and the real pipeline passed twice when Nightly patched those two lines in
  memory. That was already true when Release held the tag on 2026-09-11 for the same reason, and nothing about it
  has changed since — no nightly has run at all in the nine days since, so there is still no fresh, real pass to
  point to. Holding again is the same call as last time, not a new one.

**Nothing here needs a new escalation.** [#12](https://github.com/Nirmaypanchal/rapport/issues/12) has tracked the
silent nightly since 09-12, is still open, and Build/Research have re-verified it accurately most days without
re-filing — per AGENTS.md, escalating twice for the same thing is not the move. Checked `sprint/needs-human/` first:
all four open files (cloud-blocks-reddit, reddit-bot-credentials, nightly-has-not-run, apple-developer-credentials)
are unchanged and genuinely still blocked on the owner. No new file.

## Docs (done regardless of the tag)

README.md, `docs/comparison.md` and `docs/integrations.md` already read true for everything shipped — Build has been
keeping them current in the same PRs (verified: the MCP `ask` tool tool table in `docs/integrations.md`, the Ask/MCP
rows in `docs/comparison.md`). One thing was stale: `docs/roadmap.md`'s Done section still described summary
templates as "per recording" only (the per-source template, #15, shipped 09-14) and the MCP bullet only said
"search and read," missing the `ask` tool (#22). Fixed both lines to say what actually shipped.

## Landing page

`site/index.html` still parses (`html.parser` sanity check, clean). Grepped `coming next`: the one remaining pill is
on People memory, which genuinely hasn't shipped — correct. The three "Download for Mac" buttons still point at
`/releases`, which is still empty with no tag ever cut — that is the direct, known cost of holding the tag again,
not a new bug; nothing to fix in code until there is a release to point at. Left the "Nothing is uploaded" hero copy
as-is: the 2026-09-11 judgement call (defensible — Rapport itself uploads nothing; the accurate MCP caveat lives in
`docs/privacy.md` and `docs/use-cases.md`, which already carry it) holds, no new information changes it this run.

## Announce and draft

None — the role file ties the r/rapport release announcement to an actual tag, and there isn't one. Community's
`sprint/reddit/failed/2026-09-14-weekly-update.md` already covers everything shipped through #15 and is Community's
to resend or supersede (still blocked only on #3, the Reddit bot credentials — unrelated to this decision).

## Needs-human

Checked first, nothing new (see above).

## Next run

- **Nightly:** the only thing that unblocks this. Needs the owner's Mac to run once and report a pass against
  current `main` — `sprint/needs-human/2026-09-12-nightly-has-not-run.md` has the exact commands.
- **Release (next Friday, or sooner if a fresh passing nightly lands first):** if the newest nightly log passes,
  release — likely a minor bump, seven features queued. Docs and landing-page work above stays valid, nothing to
  redo except adding the release section to `CHANGELOG.md` and drafting the announcement.

**Escalations:** none new.
